# Объявляем функцию find_median для поиска медианы, в качестве аргумента передаем введенную пользователем строку.

def find_median(input_string):

  # создаем пустой список, в который будем помещать числа из пользовательского ввода.
    numbers = []
    # в цикле пробегаем по введенной строке и, заодно, отлавливаем возможные ошибки ввода, корректные данные добавляем в созданный список.

    for word in input_string.split():
        try:
            number = float(word.replace(',', '.'))
            numbers.append(number)
        except ValueError:
            print('Некорректный ввод.')
            return
    # также включаем проверку на то, что полученный список содержит хотя бы одно число.

    if len(numbers) == 0:
        print('Некорректный ввод.')
        return

    # для поиска медианы сортируем список по возрастанию при помощи метода .sort, находим его длину и проверяем, содержит он четное или нечетное количество элементов.
    # в зависимости от этого применяем подходящую формулу для расчета медианного значения.

    numbers.sort()
    n = len(numbers)
    if n % 2 == 0:
        median = (numbers[n//2 - 1] + numbers[n//2]) / 2
    else:
        median = numbers[n//2]

    # Выводим полученный результат на экран

    print('Median:', median)

input_string = input('Введите последовательность чисел: ')
find_median(input_string)
